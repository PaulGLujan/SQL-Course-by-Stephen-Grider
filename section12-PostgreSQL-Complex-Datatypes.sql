-- Prints 12
SELECT (12.3242342325::INTEGER);

-- Prints ads
SELECT ('adsfdasfas'::CHAR(3));

-- Prints a with whitespaces
SELECT('a'::CHAR(3));

-- Prints a without whitespaces
SELECT('a'::VARCHAR(3));